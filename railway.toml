[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn \"backend.app:app\" --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
